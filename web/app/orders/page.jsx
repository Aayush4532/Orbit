import { PackageCheck, ReceiptText } from "lucide-react";
import { redirect } from "next/navigation";
import { DashboardNav } from "@/components/orbit-nav";
import { getSession } from "@/lib/session";
export default async function OrdersPage() { if (!(await getSession())) redirect("/login"); return <main className="min-h-screen bg-[#0a0a0a] text-white"><DashboardNav active="Orders" /><div className="mx-auto max-w-4xl px-5 py-10 sm:px-8"><p className="text-xs font-semibold uppercase tracking-[.18em] text-lime-300">Purchase history</p><h1 className="mt-3 text-4xl font-semibold tracking-[-.05em]">Your orders.</h1><div className="mt-10 rounded-3xl border border-white/10 bg-[#121212] p-8 text-center"><div className="mx-auto grid h-12 w-12 place-items-center rounded-2xl bg-lime-300/10 text-lime-300"><PackageCheck size={22} /></div><h2 className="mt-5 text-lg font-semibold">Nothing secured yet</h2><p className="mx-auto mt-2 max-w-sm text-sm leading-6 text-zinc-500">When you buy a product within a live event, its confirmation and tracking status will appear here.</p></div><div className="mt-6 flex items-center gap-3 rounded-2xl border border-white/10 p-5 text-sm text-zinc-500"><ReceiptText className="text-lime-300" size={18} /> Orders are served through <code className="text-zinc-300">/api/orders</code>, ready to connect to your persistent backend.</div></div></main>; }
